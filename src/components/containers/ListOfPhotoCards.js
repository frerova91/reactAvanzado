//aqui tendira que esta un componente que se encarga de hacer el fecth y unir el hoc con su componente presentacional de real debido a la estructura de apollo en su ultima version no lo estamos usando.

/* import { withPhotos } from '../hoc/withPhotos'
import { ListOfPhotoCardsComponent } from '../components/ListOfPhotoCards'

export const ListOfPhotoCards = withPhotos(ListOfPhotoCardsComponent) */
